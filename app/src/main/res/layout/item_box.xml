<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/black"
    android:layout_marginVertical="@dimen/minMargin"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/imgCastle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_castle_2"
        android:layout_margin="@dimen/commonMargin"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
    <TextView
        android:id="@+id/tvTxt"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:text="asdasd sad"
        android:textColor="@color/white"
        android:layout_marginHorizontal="@dimen/commonMargin"
        app:layout_constraintStart_toEndOf="@id/imgCastle"
        app:layout_constraintEnd_toStartOf="@id/tvPrice"
        app:layout_constraintTop_toTopOf="@id/imgCastle"
        app:layout_constraintBottom_toBottomOf="@id/imgCastle"
        />
    <TextView
        android:id="@+id/tvPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="30,0323€"
        android:textColor="@color/white"
        android:layout_marginEnd="@dimen/commonMargin"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/imgCastle"
        app:layout_constraintBottom_toBottomOf="@id/imgCastle"/>
</androidx.constraintlayout.widget.ConstraintLayout>